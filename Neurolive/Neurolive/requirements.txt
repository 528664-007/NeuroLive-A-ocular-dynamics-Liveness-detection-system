# Core
torch>=2.2
numpy>=1.26
scipy>=1.12
scikit-learn>=1.4

# Phase 1 SCNN baseline
snntorch>=0.9

# Phase 2 joint model — optional, code auto-falls-back if this fails to
# build (needs a working CUDA toolchain; see README hardware notes).
# Comment out and rerun `pip install -r requirements.txt` if it fails.
mamba-ssm>=1.2; sys_platform == "linux"

# Phase 5 demo backend
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6

# Phase 6 report/plots
matplotlib>=3.8

# Dev / tests
pytest>=8.0
